{
  "name": "horario-paradise-pass",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/generate-config.mjs",
    "lint": "node --check js/app.js 2>/dev/null || echo lint-skip",
    "serve": "npx --yes serve . -p 8787"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
